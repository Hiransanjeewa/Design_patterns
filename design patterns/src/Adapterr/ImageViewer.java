package Adapterr;

//Assume that this class is written by someone else
// Doesn't implements the player class as well
// this class in incompatible with the player interface and you cannot change this class

public class ImageViewer {
    public void show(String filepath){
        System.out.println("Playing the img file -> "+filepath);
    }
}
